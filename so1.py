import po2
from tkinter import *
import cv2 as cv
#window=Tk()
#window.title("Welcome")q
#window.mainloop()

po2.detectCount('tf0.mp4')

"""cv.imshow('out1',out)
if cv.waitKey(10) & 0xFF == ord('q'):
    
    out1.release() # Destroys the capture object
    
    cv.destroyAllWindows() # Destroys all the windows"""